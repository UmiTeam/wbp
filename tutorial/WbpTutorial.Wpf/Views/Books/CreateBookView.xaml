<UserControl x:Class="WbpTutorial.Wpf.Views.Books.CreateBookView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WbpTutorial.Wpf.Views.Books"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <TextBlock Text="Name"></TextBlock>
        <TextBox Text="{Binding Book.Name}"></TextBox>
        <TextBlock Text="Publish Date"></TextBlock>
        <TextBox Text="{Binding Book.PublishDate}"></TextBox>
        <TextBlock Text="Price"></TextBlock>
        <TextBox Text="{Binding Book.Price}"></TextBox>
        <Button Content="Create" Command="{Binding CreateBookCommand}"></Button>
    </StackPanel>
</UserControl>